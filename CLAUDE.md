# Sourcerer

> A hybrid RAG knowledge assistant that answers from your documents — with sources.

Persistent project context for Claude Code. Read this at the start of every session.

## What this project is

**Sourcerer** is an internal-knowledge assistant that answers questions grounded in a document
corpus, built as a **portfolio project** to demonstrate production LLM-engineering skills.
The name plays on *source* + *sorcerer*: every answer is grounded in cited sources.
The defining features (the things that make it stand out) are:

1. **Hybrid retrieval** — vector + keyword (BM25) + reranker, not vector search alone.
2. **Evaluation harness** — measurable proof the system works, not "looks fine to me".
3. **Hybrid routing** — easy/sensitive/high-volume queries → local LLM; hard queries → frontier API.

Full design rationale and phase plan live in `docs/hybrid-rag-portfolio-project.md`. Read it before
making architectural decisions.

## Goal & priorities

- This is a learning + portfolio project, not a product. Optimize for **clarity and demonstrable
  engineering judgment**, not feature count.
- Every quality claim must be backed by an eval number. No "this should be better" without measuring.
- Prefer writing core logic explicitly first (to show understanding), then introduce frameworks.

## Target stack

| Layer            | Choice                                              |
|------------------|-----------------------------------------------------|
| Local inference  | Ollama (dev) → vLLM (production)                    |
| Local model      | Qwen 2.5 32B or Llama 3.x, INT4 quantized           |
| API model        | A frontier API model, for the "hard query" route    |
| Vector DB        | pgvector (start here) — Qdrant/Weaviate if scaling   |
| Keyword search   | BM25 (Postgres FTS or OpenSearch)                   |
| Reranker         | bge-reranker (local) or a rerank API                |
| Embeddings       | bge-m3 (local, keeps the hybrid theme consistent)   |
| Orchestration    | Write explicitly first; LlamaIndex if it earns its place |
| Backend          | FastAPI                                             |
| Eval             | RAGAS or a hand-written harness                     |
| Packaging        | docker-compose; one command to run the whole stack  |

## Architecture (target)

```
Documents → Ingestion (chunk + embed + index)
Query → Hybrid Retrieval (vector + BM25 → RRF fusion → reranker)
      → Router (simple/sensitive → local LLM; hard → API)
      → Generation with citations
      → Eval harness + logging/observability
```

Optional Phase 6 extension: GraphRAG as a *parallel* retrieval path for global/whole-corpus
questions, routed separately from hybrid retrieval, with eval comparing the two.

## Build order (phases)

Work strictly in order. Get each phase end-to-end working before improving quality.

- **Phase 1** — MVP: ingestion + vector-only retrieval + generation with citations, behind FastAPI.
- **Phase 2** — Hybrid retrieval: add BM25, fuse with RRF, add reranker, experiment with chunking.
- **Phase 3** — Evaluation harness (most important): retrieval metrics (recall@k, MRR, hit rate)
  and generation metrics (faithfulness, answer relevancy via LLM-as-judge). Comparison tables.
- **Phase 4** — Hybrid routing: classify queries, route local vs API, track cost/latency, compute savings.
- **Phase 5** — Production polish: migrate to vLLM, observability, guardrails (refuse when no answer
  in context), dockerize, write a strong README.
- **Phase 6 (optional)** — GraphRAG extension (see blueprint).

Update the "Current status" section below as phases complete.

## Conventions

- Language: Python. Type hints required. Format with ruff/black.
- Keep retrieval, routing, generation, and eval as cleanly separated modules.
- All LLM calls go through one thin client wrapper (so local vs API is swappable).
- Log every query: retrieval trace, which route was taken, tokens, latency, cost.
- Secrets in `.env`, never committed. Provide `.env.example`.
- Each phase merges only when its eval numbers are recorded.

## Hard rules

- **Never skip evaluation.** A change isn't "better" until the harness says so.
- When the answer isn't in the retrieved context, the system says "I don't know" — it does not guess.
- Start the local model with Ollama; only move to vLLM in Phase 5.
- Keep the corpus small while iterating (especially before any GraphRAG indexing — it's expensive).

## Current status

- [x] Phase 1 — MVP (ingestion + vector retrieval + cited generation behind FastAPI POST /query)
- [x] Phase 2 — Hybrid retrieval (BM25 FTS ∥ vector → RRF → reranker; swappable chunking; vector path kept behind RETRIEVAL_MODE flag)
- [x] Phase 3 — Evaluation harness (recall@k/MRR/hit + faithfulness/answer-relevancy via LLM judge; vector vs hybrid vs hybrid+rerank table in README)
- [x] Phase 4 — Hybrid routing (heuristic difficulty router + privacy override → local vs frontier API; per-query route/tokens/latency/cost logged to query_log; `scripts/route_report.py` reports split + % saved vs API-only; API route via official anthropic SDK, gateway-aware base_url; Thai-aware markers)
- [x] Phase 5 — Production polish (vLLM backend swappable via LOCAL_BACKEND behind the LLM wrapper; embeddings stay on Ollama; structured per-query JSON trace in observability/trace.py; guardrails: empty/low-relevance context → "I don't know" + prompt-injection screen; docker-compose runs db+ollama+api, Dockerfile installs .[api]; full README per blueprint)
- [x] Phase 6 — GraphRAG (optional): src/sourcerer/graphrag/ (extraction→graph/communities→summaries; local + global map-reduce search defined in graphrag/search.py, but ONLY global_search is wired into /query — local_search is implemented/exported yet not exposed on the endpoint. route_override graph-local/graph-api BOTH run global search; "local/api" picks where the reduce step runs, not the search mode). Graph store swappable via GRAPHRAG_STORE behind graphrag/store.py (json default — whole graph in memory under GRAPHRAG_ROOT, simple/inspectable for small corpora | postgres — graphrag/pgstore.py ranks community summaries via pgvector HNSW, O(k) reads instead of O(whole graph) at scale, reusing the existing Postgres + embedder), mirroring the LOCAL_BACKEND / SQL_KB_BACKEND pattern. Indexing uses the LOCAL model (GRAPHRAG_EXTRACTION_MODEL) and is gated behind a confirm prompt (scripts/graphrag_index.py — EXPENSIVE). Router sends overview questions → graph global via /query when GRAPHRAG_ENABLED. scripts/graphrag_eval.py compares vs hybrid (quality + cost) on eval/overview_eval_set.jsonl. networkx is the [graphrag] extra. NOTE: index has NOT been built yet (needs the expensive run).
- [x] Phase 7 — SQL knowledge base (optional): src/sourcerer/sqlkb/ treats a SQLite DB (SQL_KB_PATH, opened read-only) as a source via two paths. Path A (RAG): scripts/ingest_sql.py runs a user SELECT, 1 row = 1 doc (source label kb:<id>), reuses the chunk→embed→store pipeline (pipeline._store_document shared with ingest_directory). Path B (Text-to-SQL): analytical questions (router.is_analytical_query, EN+Thai markers) → generate read-only SELECT → safety.safe_select validates (single SELECT/WITH, no DML, enforces LIMIT) → execute → cited NL answer (SQL+rows = the citation). Wired in api/routes.py via _text_to_sql_query (auto when SQL_KB_ENABLED + analytical, or route_override="sql"). scripts/build_sql_demo.py seeds a demo sales DB; scripts/sql_eval.py reports execution accuracy on eval/sql_eval_set.jsonl. Gated behind SQL_KB_ENABLED. Storage engine is swappable via SQL_KB_BACKEND behind sqlkb/backends.py (sqlite default | duckdb columnar for analytics-scale tables — aggregations push down; introspection via information_schema; [duckdb] extra), mirroring the LOCAL_BACKEND pattern. Runtime cost guards (backends.query_guard, used by text_to_sql._execute): wall-clock SQL_KB_TIMEOUT_S aborts a runaway query on both engines (SQLite progress-handler deadline; DuckDB watchdog-thread interrupt) and SQLite-only SQL_KB_MAX_SCAN_OPS budgets VM ops as a rows/bytes-scanned proxy — a trip raises QueryCostError → "I don't know". Schema retrieval (sqlkb/schema_retrieval.py, used by text_to_sql.run): when a source has more than SQL_KB_SCHEMA_TOP_K tables, only the most relevant ones go in the prompt instead of every table's DDL — tables ranked by lexical overlap + bge-m3 embedding cosine, fused with RRF (same hybrid idea as document retrieval); ranks on cheap name+column signatures (schema.table_signatures), then builds full descriptions w/ sample rows for survivors only (describe_schema(..., tables=)). Embedding leg is best-effort (degrades to lexical); 0 or table count ≤ top_k = describe all (unchanged behaviour). scripts/sql_eval.py now reports, beside execution accuracy, the operational numbers the scale work moves: end-to-end latency (mean/median/p95), SQL-gen tokens/query, prompt-schema narrowing (mean prompt_tables/total — SQLExecution.prompt_tables exposes per-query table count), and cost-guard aborts. Demo run (qwen2.5 + 1-table demo DB): 6/6 accuracy, ~1s median latency, 1/1 tables (no narrowing on a 1-table schema), 0 guard aborts.
- [ ] Phase 8 — Orchestration (Dagster, optional, PLANNED — design only): wrap the existing scripts/ entrypoints (ingest, ingest_sql, graphrag_index, run_eval) as software-defined assets (raw_corpus→chunks→[graph_index]→eval_report) with schedules/sensors/partitions; reuse the existing idempotent ingest as re-run safety; Dagster run metadata + everything stays on the existing Postgres. Chosen over Airflow for asset lineage (fits the answer-with-sources theme). Design doc: docs/orchestration-analytics-phase-8-9.md. NOT built yet.
- [ ] Phase 9 — Analytics layer (dbt, optional, PLANNED — design only): dimensional model over query_log (the existing per-query log = raw fact table): staging → fct_queries/fct_eval_runs + dim_route/model/date → marts (cost_daily, route_split, latency_by_route, eval_trend); dbt tests for data quality; fct_queries incremental; Dagster runs `dbt build` as a downstream asset. Design doc: docs/orchestration-analytics-phase-8-9.md. NOT built yet.
- [x] Phase 10 — Data governance (Cerbos, optional): authorize at retrieval/execution time, not generation (forbidden chunks/rows/communities never enter the candidate set). Gate = SQLGlot (parser) + Cerbos (PDP): Cerbos is a pure decision fn over (principal, resource, action) — it doesn't parse SQL, so SQLGlot is the bridge that extracts tables/cols from generated SQL → enriched from catalog → fed to Cerbos. 3 layers: catalog (asset_catalog in existing Postgres → OpenMetadata as CATALOG_BACKEND swap; classification/owner_team/pii_tags) → governance gate (SQLGlot parse → Cerbos CheckResources/PlanResources; sqlkb/safety.py regex stays as the read-only guard) → data plane (Postgres RLS, defense-in-depth backstop). Doc RAG (no parse): Cerbos PlanResources→WHERE source filter pushed into vector/BM25 (same predicate-pushdown idea as Phase 7). SQL KB: SQLGlot parse generated SQL → table/column policy check → reject forbidden table / mask PII column (SQLGlot rewrite) / splice row-filter; optional pre-filter schema in prompt + RLS backstop = defense-in-depth. Covers all 3 paths (RAG/SQL KB/GraphRAG) + scoped MCP discovery. Access model RBAC+ABAC+PII tags; principal via X-Principal header/JWT stub. Cerbos PDP = one sidecar in docker-compose; SQLGlot = [governance] extra; policies as versioned YAML. Eval (mandatory): leakage rate (headline=0) + over-restriction/false-denial (recall@k per principal) + gate metrics. Design doc: docs/governance-phase-10.md. Build order = sub-phases 10a→10d. **10a DONE** (branch feat/governance-phase-10a): foundation only, no enforcement — `src/sourcerer/governance/` (catalog.py = `asset_catalog` table [kind/asset_id → classification/owner_team/pii_tags[], classification lattice public<internal<confidential<restricted, unknown sorts most-restrictive] + CRUD; principal.py = `principals` stub-IdP table + Principal RBAC/ABAC attrs, `resolve()` is fail-closed → ANONYMOUS [public clearance] on missing/unknown header or unprovisioned table). Config: GOVERNANCE_ENABLED (default off) / CATALOG_BACKEND (local|openmetadata) / CERBOS_ENDPOINT (unused till 10b) / PRINCIPAL_HEADER (X-Principal). Route resolves principal from header (None when governance off) → recorded on query_log (new `principal` col) + JSON trace only. `scripts/ingest.py --classification/--owner-team/--pii` tags ingested sources (pipeline now returns `sources`); `scripts/catalog.py` manages assets+principals (set/list/seed-demo). 12 pure-logic tests (no DB). **10b DONE** (branch feat/governance-phase-10b): document-RAG gate, enforced at retrieval time — `governance/policy.py` = the access rule written explicitly (admin OR clearance≥classification OR owner_team∈teams; the reference all other layers mirror); `governance/gate.py` = PDP swappable via GOVERNANCE_PDP [local = policy.can_read in-process, default, runs eval/tests no-infra | cerbos = externalised Cerbos sidecar over policies/document.yaml]; `allowed_document_sources(principal)` resolves the visible source set → pushed into retrieval as `WHERE source = ANY(...)` via `retrieval/filter.py.source_clause`, fed into vector.py + bm25.py + hybrid + retriever (None=no filter, empty set=nothing visible→"I don't know", never leak). Route computes the gate set + appends "governance: N/M sources visible" to the logged reason. Cerbos sidecar added to docker-compose (`cerbos/config.yaml` + `policies/document.yaml`, CERBOS_ENDPOINT), [governance] extra = cerbos+sqlglot, GOVERNANCE_DEFAULT_CLASSIFICATION (untagged source default, public). Eval: `scripts/build_governance_demo.py` seeds a 4-doc corpus (dummy embeddings, no Ollama) + principals; `scripts/governance_eval.py` reports leakage (gated vs ungoverned baseline) + recall@k per principal on `eval/governance_eval_set.jsonl`. **Demo run: leakage 5/5=100% (baseline) → 0/5=0% (gated); authorized recall 7/7=100% preserved. Cerbos parity verified 6/6 against the live sidecar.** +11 tests (policy/PDP/gate planner), full suite 136 passing. **10c DONE** (branch feat/governance-phase-10c): SQL-KB gate, enforced on generated SQL — `governance/sql_gate.py` parses the LLM's SQL with **SQLGlot** (reliable where safe_select's regex isn't: `created_at` never trips "create"), then `gate_sql(sql, principal, schema=)`: forbidden TABLE → reject whole query → "I don't know"; unreadable PII COLUMN → SQLGlot **rewrites** the projection to `'***'` (named cols, `SELECT *` expanded from schema, and aggregates like `avg(salary)` all masked; multi-table/no-schema star → fail-closed reject). Decision delegated to the SAME swappable PDP as doc-RAG (`gate.get_pdp` made kind-aware — local reference | Cerbos sidecar over policies/{table,column}.yaml), so one policy governs all three paths. Wired into `sqlkb/text_to_sql.run`→`answer.answer`→route `_text_to_sql_query` (threads the Principal; `safe_select` stays as the read-only guard *before* the gate); SQLExecution gains gate_rejected + masked_columns; cited SQL shows the mask. Eval: `scripts/sql_gate_eval.py` runs a canned (principal, SQL) matrix offline (no LLM) on `eval/sql_governance_eval_set.jsonl`, leak-checks the *rewritten* SQL independently. **Demo run: leakage 5/10=50% (baseline) → 0/10=0% (gated); 2 tables rejected, 4 PII cols masked, gate p95 ~20ms. Cerbos loads 3 policies; table/column parity verified against the live sidecar.** +8 SQLGlot tests, full suite 144 passing. **10d DONE** (branch feat/governance-phase-10d, core scope — RLS + MCP discovery intentionally deferred per the doc's "optional depth"): closes the 3rd path + audit. `gate.filter_readable_communities(communities, principal)` drops any GraphRAG community whose sources aren't ALL readable (a summary fuses several sources → subset rule, fail-safe), wired into route `_graph_global_query` before map/reduce (threads the Principal). Audit: new `query_log.denied_assets` column + trace field record how many assets the gate hid (doc path = total−visible; graph path = communities filtered); router_reason carries the human note. Also exports `allowed_document_sources` + `filter_readable_communities` from the governance package (fixed a latent 10b bug: the route referenced `governance.allowed_document_sources` which wasn't exported — uncaught because the full /query path needs Ollama). +2 graph-gate tests (no index needed), full suite 146 passing. Live check vs real Postgres: doc gate escalates correctly anonymous(public-only)→alice(+internal)→bob(+hr-team confidential)→carol(admin=all). **RLS backstop DONE** (defense-in-depth, GOVERNANCE_RLS_ENABLED, off by default): `scripts/setup_rls.py` provisions a restricted non-superuser reader role + `governance_rank()` SQL fn (lattice parity) + RLS policy `chunks_reader_acl` that **recomputes policy.can_read inside Postgres** from asset_catalog + principals + the `sourcerer.principal` session GUC — so an app-gate bug can't widen DB results (owner is superuser → bypasses RLS, governance-off path unchanged). `db.session.connect(reader_principal=)` switches to the reader + sets the GUC, threaded through retriever/hybrid/vector/bm25; route uses it when GOVERNANCE_RLS_ENABLED. `scripts/rls_demo.py` proves it: with the app filter fully disabled, the reader sees ONLY authorized rows (0 leaks; anon→faq only … carol→all). .env.example gained the full Phase-10 block. **Phase 10 COMPLETE (10a–10d + RLS backstop); MCP scoped discovery is the one remaining documented optional extension.**
- [x] CAG PoC — Cache-Augmented Generation governed by Cerbos (branch feat/cag-cerbos-poc, PR #11 stacked on Phase 10, optional/PoC — NOT a numbered phase): move the Cerbos gate from retrieval time to **cache-build time** so forbidden content never enters the context without any per-query gate. `src/sourcerer/cag/` (cache.py = build + memoize a per-access-profile context cache, gate runs at build; generate.py = answer from the cache, no retrieval). Caches keyed by the allowed-source set → principals with equal access share one cache (per-tier, not per-user). CAG reads **whole documents straight from disk** (`CAG_CORPUS_DIR`, basename = source/asset id), not the `chunks` table — it wants full text and never embeds, so it needs neither chunking nor the ingestion pipeline; the same catalog tags + Cerbos policy still govern it. **PlanResources predicate pushdown** behind `GOVERNANCE_PUSHDOWN` (needs GOVERNANCE_PDP=cerbos): default gate enumerates every candidate source → Cerbos CheckResources (payload grows with corpus); pushdown asks Cerbos **PlanResources once** and translates the returned plan AST → SQL `WHERE` (`governance/plan.py`, `_translate` maps eq/in/and/or → `= %s` / `= ANY(%s)` / connectives) — one round-trip, cost independent of corpus size (same predicate-pushdown idea as Phase 7, taken up a layer: the *policy* becomes the predicate). Enabling it required a policy refactor: precompute `Principal.readable_classifications` (every level ≤ clearance) so the Cerbos rule is plain `classification in [...]` membership instead of rank arithmetic over a map → PlanResources emits a clean `IN` plan → SQL `= ANY`. local / CheckResources / plan stay in parity. `cag/cache._gate_sources` dispatches on the flag and intersects the result with the on-disk universe (plan authorizes over the indexed `chunks` corpus, which can differ from disk; falls back to CheckResources when pdp=local). Eval/proof: `scripts/cag_poc.py` (live demo), `scripts/plan_parity.py` (3-way local/CheckResources/PlanResources parity vs the live sidecar), `scripts/build_governance_demo.py` now also writes the demo corpus to disk for CAG. **Live run: plan_parity PASS — all 3 PDP paths agree for anonymous(faq)→alice(+security)→bob(+handbook)→carol(all); cag_poc through pushdown partitions caches identically + governance preserved (anon/alice "I don't know" on salary, bob/carol answer from handbook.md), cache built once & reused 6×.** +4 CAG gate-dispatch tests + translator/lattice tests, full suite 156 passing. .env.example gained GOVERNANCE_PUSHDOWN + CAG_CORPUS_DIR. Design note: docs/cag-cerbos-poc.md. Not production: cache invalidation on policy/tag change, real KV-prefix caching (vLLM/llama.cpp), large-corpus handling — out of PoC scope.
- [x] Confluence ingestion (optional source, NOT a numbered phase): a third ingest source alongside directory files (Phase 1) and SQLite rows (Phase 7), sharing the same `_store_document` chunk→embed→store step so retrieval/citations don't care where a chunk came from. `src/sourcerer/ingestion/confluence.py`: `iter_pages(cql, ...)` — a CQL query defines what to ingest (same idea as SQL KB's user-written SELECT), 1 page = 1 document, source label `confluence:<page_id>` (stable across title edits/moves); `storage_to_text` strips Confluence's XHTML "storage" format down to plain text via stdlib `html.parser` (no new dependency — httpx was already a base dep, used here for Confluence Cloud basic auth [email + API token] and pagination). Confluence Cloud paginates content search via an opaque cursor in `_links.next`, not a numeric `start` offset — a real bug found during live testing (naive offset increments re-fetched the same first batch forever); fixed by following `_links.next` until absent. `iter_pages` yields `(source, text, metadata)` 3-tuples — `_page_metadata()` shapes the same response's other fields (no extra request; `expand` just widens it) into: title/type/status, space (key/name), the clickable webui `url`, version number + last-modified(-by), created(-by), the page's ancestor breadcrumb, and labels — filtering out noise like avatars/ARIs/`_expandable` stubs. Stored in a new `document_metadata` table (`source TEXT PRIMARY KEY, metadata JSONB, updated_at`, schema in `db/session.py`) via `pipeline._store_metadata`; `pipeline.ingest_confluence` has its own store loop (not the shared `_ingest` used by the other two sources, since only Confluence has real per-document metadata to persist). `scripts/ingest_confluence.py` is the CLI (`python scripts/ingest_confluence.py "space = ENG and type = page"`), carrying the same `--classification/--owner-team/--pii` governance-tagging flags as `scripts/ingest.py`. Config: `CONFLUENCE_BASE_URL/EMAIL/API_TOKEN/MAX_PAGES` (the last is a safety cap on pages pulled per run, mirroring `sql_kb_max_rows`). `client=` param on `iter_pages` is a test seam (httpx.MockTransport) — no live Confluence needed for the tests in `tests/test_confluence.py`. Ingestion-only: no runtime routing, no text-to-Confluence-query path (unlike SQL KB's Path B); `document_metadata` isn't surfaced in citations/API yet, just stored. **Live-tested against the real `QA` space** (scbtechx.atlassian.net, 2100+ pages, 50-page test batch): 44 pages / 48 chunks ingested, verified end-to-end through the actual FastAPI + web UI (not just curl) with correct grounded, cited answers; `document_metadata` populated for all 44 sources with real URLs/authors/dates/breadcrumbs. Full suite 168 passing.

- [x] Confluence document data quality (optional): per-document DQ score over the ingested Confluence corpus via `scripts/dq_report.py`, two stages. Stage 1 (default, no LLM): metadata checks — staleness/orphaned-owner/unlabeled/never-reviewed/orphan-page/bad-status — pure SQL over `document_metadata`, plus stub detection (word count via `chunks`) and near-duplicate detection (cosine similarity over per-source mean chunk embeddings already in pgvector, O(n²) fine at this corpus size, no re-embedding). Stage 2 (`--with-llm`): residual-tag/entity regex (catches `storage_to_text` extraction bugs) + staleness-marker regex (TODO/deprecated/etc.), plus an LLM-as-judge clarity/currency score — same "ask for a single number" pattern as the Phase 3 eval judge (`eval/generation_metrics.py`). Note: the design doc's original markup-residue sketch (consecutive-short-lines) doesn't work because `chunking.chunk_text` whitespace-joins on ingest — newlines never reach `chunks.content` — so Stage 2 checks residual tags/entities instead, which don't depend on newlines. `DocScore`/`render_report`/`_dq_dict` are pure functions (19 tests, no DB/LLM needed); the SQL/embedding fetchers + `_persist_scores` are exercised live only. `--persist` writes each document's dq object into `document_metadata.metadata->'dq'` (queryable via plain SQL/DBeaver without re-running the report) via `jsonb_set`, fully replacing the prior `dq` object each run — a `--persist` run without `--with-llm` drops the Stage 2 fields rather than merging stale ones back in, so the stored dq always matches exactly what that run printed. **Live run against the real 44-page QA-space corpus**: Stage 1 — 42/44 stale (>2y), 43/44 orphaned owner, 44/44 unlabeled, 5/44 never-reviewed (v1), 19/44 stub (<50 words), 0 orphan pages, 0 duplicates; avg 3.5 issues/doc. Stage 2 (qwen2.5:7b via Ollama, ~95s/44 docs): 3/44 markup residue, 0/44 staleness markers, avg LLM quality score 0.59. Persisted and verified: `document_metadata.metadata->'dq'` populated for all 44 sources, queryable e.g. `(metadata->'dq'->>'issue_count')::int`. Design doc: docs/confluence-data-quality.md. Not wired back into retrieval/citations yet (scored but not filtered — that's the documented next step).

(Claude Code: keep this checklist current as work progresses.)
